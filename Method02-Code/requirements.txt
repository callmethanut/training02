playwright>=1.40
openpyxl>=3.1

# After `pip install -r requirements.txt`, also run once to download the
# Chromium binary that Playwright drives (~150 MB, one-time):
#     python -m playwright install chromium
